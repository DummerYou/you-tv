# 原版行为对照表

| 功能 | 原版行为 | 新版要求 | 验证方式 |
| --- | --- | --- | --- |
| TXT 分组 | `组名,#genre#` 切换当前分组 | 保持一致 | `PlaylistParserTest` |
| 同名频道 | 同组同名频道合并播放地址 | 保持来源顺序 | `PlaylistParserTest` |
| M3U | 读取名称、台标、频道号、分组和 HTTP Headers | 保持一致 | `PlaylistParserTest` |
| JSON | 读取原 `TV` 字段 | 保持兼容 | 单元测试 |
| IPv6 视频 | 原实现依赖 `Uri`/Media3，未专项验证 | 保留带方括号的原始地址 | 单元测试和真机播放 |
| 收藏 | 按原频道顺序 ID 保存 | 首次迁移后改用稳定 ID | 数据迁移测试 |
| 换台 | 上下键受 `channel_reversal` 控制 | 保持一致 | UI 测试 |
| 多源 | 当前源失败后尝试下一地址 | 保持顺序，记忆成功源 | 播放器测试 |
| EPG | 按频道标准名包含匹配 | 保持一致 | Repository 测试 |
| 远程配置 | 34567 端口无鉴权 | 字段兼容，改为限时开启 | 服务测试 |
